#!/usr/bin/env python3
"""
GStreamer Pipeline Formatter - Format long GStreamer pipelines for readability.

This tool formats GStreamer pipelines by breaking them into multiple lines
at appropriate element boundaries to improve readability.
"""

import sys
import os

# Add the parent directory to Python path to import hailo_utils
sys.path.insert(0, os.path.join(os.path.dirname(__file__), '..'))

from hailo_utils.pipeline.formatter import main

if __name__ == '__main__':
    main()