#!/usr/bin/env python3
"""
Git Repository Information - Extract git repository details.

This tool displays information about the current git repository including
remote URL, branch, repository name, and protocol type.
"""

import sys
import os

# Add the parent directory to Python path to import hailo_utils
sys.path.insert(0, os.path.join(os.path.dirname(__file__), '..'))

from hailo_utils.core.git_utils import main

if __name__ == '__main__':
    main()